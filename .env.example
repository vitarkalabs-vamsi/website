# Optional — only needed if the API is hosted on a different origin than the
# static site. Leave unset for same-origin requests (the normal Cloudflare
# Worker setup, where the Worker serves both the site and /api/*).
# VITE_API_BASE=https://api.vitarkalabs.com
